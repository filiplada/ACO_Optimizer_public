
<UserControl x:Class="ACO_Optimizer.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400"><Grid>
    <StackPanel Margin="20">
        <TextBlock Text="Ustawienia ACO" FontWeight="Bold" FontSize="16" />
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Alpha:" Width="100" />
            <TextBox Text="{Binding Parameters.Alpha}" Width="100" />
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Beta:" Width="100" />
            <TextBox Text="{Binding Parameters.Beta}" Width="100" />
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Evaporation:" Width="100" />
            <TextBox Text="{Binding Parameters.EvaporationRate}" Width="100" />
        </StackPanel>
    </StackPanel>
</Grid>
</UserControl>
